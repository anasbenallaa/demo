# --- VCS / editor / tooling metadata --------------------------------------
.git
.gitattributes
.gitignore
.github
.cursor
.junie
.agents
.idea
.vscode
.zed
.nova
.fleet
.editorconfig

# --- Secrets / local env -------------------------------------------------
.env
.env.*
!.env.example
auth.json

# --- Dependencies (reinstalled inside the image) -----------------------
/vendor
/node_modules

# --- Generated Wayfinder client (regenerated by `npm run build`) --------
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder

# --- Build / runtime output (rebuilt inside the image) -----------------
/public/build
/public/hot
/public/storage
/public/fonts-manifest.dev.json
/bootstrap/cache/*.php
/storage/*.key
/storage/pail
/storage/logs/*
/storage/framework/cache/*
/storage/framework/sessions/*
/storage/framework/views/*
/database/*.sqlite
/database/*.sqlite-*

# --- Tests / static analysis (not needed at runtime) ------------------
/tests
phpunit.xml
/.phpunit.cache
/.phpunit.result.cache
phpstan.neon
pint.json

# --- Docker / docs ----------------------------------------------------
Dockerfile
docker-compose*.yml
.dockerignore
AGENTS.md
boost.json
*.md

# --- FrankenPHP local artifacts -------------------------------------
frankenphp
frankenphp-worker.php
**/caddy
.DS_Store
